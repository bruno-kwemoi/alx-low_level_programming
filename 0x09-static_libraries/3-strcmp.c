#include <string.h>
/**
 * _strcmp - compares two strings
 *@s1: pointer to the the first string
 *@s2: pointer to the second string
 *Return: result of comparison
 */

int _strcmp(char *s1, char *s2)
{
	int a = strcmp(s1, s2);

	return (a);
}
